# testvpc

A simple stack to quickly create a vpc for testing and then delete it.

to get started:

* git checkout
* create a terraform.tfvars file with needed variables
* terraform init
* terraform plan
* terraform apply

when you're done:

* terraform destroy
